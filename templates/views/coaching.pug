extends ../layouts/default

block content
  .container-fluid
    .row.text-center
      h1 Coaching with Cheyenne
    .row.text-center
      .col.d-flex.justify-content-between
        each package in locals.packages
          .postcard
            h3 #{package.title}
            hr
            h5 #{package.tagline}
            p
              !=package.details
            h5 #{package.price}
            
  